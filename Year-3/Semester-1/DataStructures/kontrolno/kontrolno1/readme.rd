zad1

zad2
It's given a implementation of iterator for forward list.
To add a function that takes as arguments: iterator, non-negative integer n and element x
that insert through n elements after the iterator x
and returns the count of inserted elements.

zad3
It's given a forward list represented as pointer to its head.
To rearrange elements so that these with reminder of 1 divided by 3
are before these with remainder of 2 deivided by 3.
Left elements are deleted. Relative order of elements is saved.